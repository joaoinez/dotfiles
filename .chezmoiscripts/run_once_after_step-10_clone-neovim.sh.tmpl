{{ if (or (eq .chezmoi.os "linux") (eq .chezmoi.os "darwin")) -}}
#!/bin/bash
set -euo pipefail

git clone https://github.com/joaoinez/kickstart.nvim.git "${XDG_CONFIG_HOME:-$HOME/.config}"/nvim
{{ end -}}